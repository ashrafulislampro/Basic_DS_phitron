#include <bits/stdc++.h>
using namespace std;

int main()
{
    // int N = 10;
    // list<int> l(N, 3);
    // list<int> l = {2, 3, 4};
    list<int> l2 = {222, 4, 44, 5, 6, 6, 6, 7, 10, 10, 22};
    // l2.sort();
    // cout << l2.front() << endl;
    // cout << l2.back() << endl;
    // cout << *next(l2.begin(), 2) << endl;
    cout << *l2.begin() << endl;
    cout << *l2.end() << endl;
    // l2.sort(greater<int>());
    // l2.unique();
    // int arr[] = {20, 30, 40};
    // vector<int> v = {20, 40, 50};

    // cout << l2.size() << " " << l2.max_size() << endl;
    // l2.assign(l.begin(), l.end());
    // l2.insert(next(l2.begin(), 2), 100);
    // l2.erase(next(l2.begin(), 2));
    // l2.erase(next(l2.begin(), 2), next(l2.begin(), 5));
    // auto it = find(l2.begin(), l2.end(), 22);
    // if (it == l2.end())
    // {
    //     cout << "Not found" << endl;
    // }
    // else
    // {
    //     cout << "Found" << endl;
    // }
    // cout << *it << endl;
    // list<int> l3(arr, arr + 3);
    // list<int> l3(v.begin(), v.end());
    // l3.clear();
    // l3.resize(2);
    // cout << (bool)l3.empty() << endl;
    // cout << l.size() << *l.begin() << endl;
    // for (auto it = l.begin(); it != l.end(); it++)
    // {
    //     cout << *it << " ";
    // }

    // for (int val : l2)
    // {
    //     cout << val << endl;
    // }
    return 0;
}